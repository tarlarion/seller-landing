# Changelog

Журнал принятых изменений. Используется как основа для сообщений коммитов.

Формат записи:
- **Дата**
- **Что изменилось**
- **Причина**
- **Детали** (если баг — в чём состоял и как решён)

---

## 2026-06-07 — Hero: убран подпись Telegram, старая цена −2pt

**Что изменилось**
- Удалена строка «подключение в Telegram» под CTA в hero.
- Старая цена: `calc(1.125rem - 2pt)` / `calc(0.875rem - 2pt)` относительно новой.

---

## 2026-06-07 — Цена: колонка и позиция ярлыка

**Что изменилось**
- Старая цена под новой, вплотную (`flex-col`, `mt-0.5`).
- Ярлык скидки выше и правее (`-top-1.5`, `translate-x-1.5`).

---

## 2026-06-07 — Фиолетовые кнопки, оранжевые ярлыки, цена без обводки

**Что изменилось**
- `--brand-primary: #5500ff` → кнопки (`--primary`); `--brand-accent: #ee5500` → ярлыки скидки.
- `ProductPrice`: убрана обводка, бейдж ближе к тексту, `bg-brand-accent`.

**Причина**
Запрос заказчика разделить цвет CTA и акcent-ярлыков, упростить блок цены.

---

## 2026-06-07 — Акцент #ee5500 и новая вёрстка цены

**Что изменилось**
- `--brand-accent: #ee5500` → `--primary` для CTA-кнопок и focus ring.
- `ProductPrice`: сначала прямоугольник с новой ценой и бейджем −62% в правом верхнем углу, справа — зачёркнутая старая цена на 2px меньше (text-lg/text-base, text-sm/text-xs).

**Причина**
Запрос заказчика на оранжевый акцент и e-commerce-паттерн отображения скидки.

**Коммит (предложение)**
```
style: orange accent and boxed discount price layout
```

---

## 2026-06-07 — Палитра, цена со скидкой, Telegram-иконка, правки UX

**Что изменилось**
- Убран eyebrow «Виртуальный помощник» из hero.
- Палитра бренда применена системно в `index.css` (shadcn tokens): Black, Nebula `#CADEE0`, Iron `#D4D7EC`, Pastel Pink `#ECD4E0`, White.
- Блок болей: вместо «Было/Стало» — перечёркнутый старый текст + новый текст.
- Компонент `ProductPrice`: ~~2 599 ₽~~ −62% **990 ₽/мес** — в header, hero, footer, финальном CTA, comparison.
- CTA-кнопки: SVG-логотип Telegram в начале, стрелка убрана.

**Причина**
Запрос заказчика на женственнее палитру (с балансом для мужчин-продавцов), акцент на скидку, более чистый паттерн до/после и узнаваемый Telegram CTA.

**Детали**
- Скидка: 2 599 → 990 ₽ (−1 609 ₽, −62%).
- Primary = чёрный для CTA; secondary/accent/muted/border — пастельные тона палитры.
- Dark mode пересобран под ту же палитру.
- Новые файлы: `telegram-icon.tsx`, `product-price.tsx`.

**Коммит (предложение)**
```
feat: apply brand palette, discount pricing, and Telegram CTA icon

Replace before/after labels with strikethrough pattern and
wire pastel shadcn tokens across the landing design system.
```

---

## 2026-06-06 — Неразрывная конструкция «в 3–5 раз» в hero

**Что изменилось**
- Заголовок hero разбит на части; фраза «в 3–5 раз» обёрнута в `whitespace-nowrap` и содержит неразрывные пробелы (`\u00A0`).

**Причина**
Конструкция «в 3–5 раз» не должна переноситься на разные строки при адаптивной вёрстке.

**Коммит (предложение)**
```
fix: keep "в 3–5 раз" on one line in hero title
```

---

## 2026-06-06 — CTA-кнопки с pill-скруглением

**Что изменилось**
- В `CtaButton` добавлен класс `rounded-full` — все CTA («Подключить бота») теперь с максимальным скруглением.

**Причина**
Запрос заказчика на pill-форму для ключевых кнопок конверсии.

**Коммит (предложение)**
```
style: use pill shape for landing CTA buttons
```

---

## 2026-06-06 — Вёрстка продающего лендинга

**Что изменилось**
- Сверстан полный лендинг по структуре из `botcontext.md`: hero, боли, как работает, метрики, аудитория, выгоды, сравнение с помощником, каналы, финальный CTA.
- Добавлены `src/content/landing.ts` (все тексты), `src/lib/constants.ts` (ссылка на бота, цена), 12 компонентов в `src/components/landing/`.
- Обновлены `index.html` (lang=ru, meta description, title) и `landing-page.tsx`.

**Причина**
Заказчик запросил вёрстку лендинга с продающими текстами и CTA в Telegram-бота по зафиксированному позиционированию.

**Детали**
- Hero: «Обрабатывайте в 3–5 раз больше заказов без найма помощников».
- Блок метрик: 5 мин vs 30 сек, экономия >7 ч/день, расчёт комиссии 20/40 заказов.
- Сравнение: помощник 60–120 тыс. ₽ vs бот 990 ₽/мес.
- CTA «Подключить бота» ведёт на placeholder `https://t.me/your_bot` — заменить в `src/lib/constants.ts`.
- Бренд временно «БайерБот» — уточнить у заказчика.
- Стиль: Toss-style, shadcn/ui, адаптив mobile-first.

**Коммит (предложение)**
```
feat: build conversion landing for buyer Telegram bot

Add hero, pains, metrics, comparison, and CTA sections with
money-focused copy from botcontext positioning.
```

---

## 2026-06-06 — Обновление позиционирования из диалога ChatGPT

**Что изменилось**
- Полностью переработан `botcontext.md`: добавлено денежное позиционирование, три ключевые боли, расчёты (55 ч/мес, 7+ ч/день, комиссия 500 ₽), сравнение с помощником (60–120 тыс. ₽ vs 990 ₽), варианты hero-заголовков, структура лендинга из 8 блоков.

**Причина**
Заказчик прислал полный текст диалога с ChatGPT. Публичный share ранее был недоступен — теперь контекст зафиксирован полностью. Ключевой сдвиг: продавать не «парсер», а **масштабирование заказов без найма** и **деньги**, а не абстрактную экономию времени.

**Детали**
- Зафиксирован рекомендуемый hero: «Обрабатывайте в 3–5 раз больше заказов без найма помощников».
- Добавлен сильный денежный оффер: «Не нанимайте помощника за 80 000 ₽. Автоматизируйте его работу за 990 ₽».
- Метрики для блока сравнения: 5 мин vs 30 сек на товар, 100 товаров/день → экономия >7 часов.
- Код лендинга не менялся.

**Коммит (предложение)**
```
docs: enrich botcontext with ChatGPT positioning and money framing

Shift landing narrative from parser features to order throughput,
helper cost comparison, and buyer income ceiling messaging.
```

---

## 2026-06-06 — Инициализация документации продукта

**Что изменилось**
- Создан `botcontext.md` — единый источник продуктового контекста: аудитория, позиционирование, сценарий бота, структура лендинга, открытые вопросы.
- Создан `changelog.md` — журнал принятых изменений для коммитов.

**Причина**
Заказчик запросил фиксировать продуктовый контекст и историю изменений до начала дальнейшей разработки лендинга. Любая следующая задача должна начинаться с чтения `botcontext.md`.

**Детали**
- Изучена ссылка [ChatGPT — «Бот для байеров РФ»](https://chatgpt.com/share/6a246312-5a28-83eb-a580-5e616ddf8329). Публичный share не отдаёт текст диалога без авторизации — в `botcontext.md` позиционирование собрано из брифа заказчика и логики ниши (байеры, Telegram-каналы, парсинг SKU, 990 ₽/мес).
- Зафиксированы требования к лендингу: hero + CTA в бота, тезисы удобства, метрики скорости, сравнение с ручным трудом, блок «как подключить».
- Код лендинга пока не менялся — остаётся заглушка «Hello, World».

**Коммит (предложение)**
```
docs: add botcontext and changelog for buyer bot landing

Document product positioning, bot workflow, and landing structure
before implementing conversion-focused pages.
```
